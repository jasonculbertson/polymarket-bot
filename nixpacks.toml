[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install py-clob-client>=0.34.0 || echo '[WARN] py-clob-client install failed — live trading disabled'"
]

[start]
cmd = "python app.py"
